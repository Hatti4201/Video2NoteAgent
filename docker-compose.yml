services:
  video-note-agent:
    build: .
    env_file:
      - .env
    volumes:
      - ./output:/app/output
      - ./secrets:/app/secrets

  telegram-bot:
    build: .
    env_file:
      - .env
    volumes:
      - ./output:/app/output
      - ./secrets:/app/secrets
    command: ["telegram"]
    restart: unless-stopped
